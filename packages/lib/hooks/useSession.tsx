"use client";

export function useSession() {
    // const [user, setUser] = useState(null);

    return {
        id: 1,
        email: "fumise@gmail.com",
        username: "funmise",
        image: "",
    };
}
